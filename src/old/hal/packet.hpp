#pragma once

//
// Created by zhouj on 2023/10/10.
//

#include "type.hpp"
#include "wait-handler.hpp"

namespace wibot {

//class SyncBus {
//   public:
//    virtual Result read(void* data, u32 size, WaitHandler& waitHandler)  = 0;
//    virtual Result write(void* data, u32 size, WaitHandler& waitHandler) = 0;
//};
//
//class AsyncBus {
//   public:
//    virtual Result listen(CircularBuffer<u8>& rxBuffer, WaitHandler& waitHandler) = 0;
//    virtual Result stop()                                                              = 0;
//    virtual Result write(void* data, u32 size, WaitHandler& waitHandler)          = 0;
//};
}  // namespace wibot
